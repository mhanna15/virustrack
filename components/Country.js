import React from "react";
import { Text } from "react-native";

import Card from "./Card";

const Country = (props) => {
  return (
    <Card>
      <Text>THE united state</Text>
    </Card>
  );
};

export default Country;
